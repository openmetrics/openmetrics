<h1>Activities</h1>

<% @activities.each do |activity| %>

    <div class="activity">
      <%#= activity.inspect %>
      <%= link_to activity.owner.username if activity.owner %>
      <%= render_activity(activity, layout: 'activity', fallback: 'default') %>
    </div>

<% end %>
