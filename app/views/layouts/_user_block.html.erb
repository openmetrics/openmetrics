
<nav class="navbar navbar-default" role="navigation">
    <!--logo-->
    <div class="navbar-header">
        <a class="navbar-brand" href="#">OpenMetrics</a>
    </div>

    <!--login / logout -->
    <div class="navbar-text navbar-right">
        <% if user_signed_in? %>
            <%= link_to "Welcome #{current_user.email}", destroy_user_session_path, { :class => "navbar-link", :method => :delete } %>
        <% else %>
            <%= link_to 'Login', new_user_session_path %>
        <% end %>
    </div>
</nav>


