<% content_for :page_header do %>
  <h3>
    <%= badge_label @project, {:label_class => 'primary'} %>
    <span class="btn-group btn-group-sm pull-right">
        <%= link_to t('om.controls.delete_html'), @project, :data => {:confirm => 'Are you sure?'}, :method => :delete, :class => 'btn btn-default' %>
        <%= link_to t('om.controls.show_html'), @project, :class => 'btn btn-default' %>
        <%= link_to t('om.controls.save_html'), 'javascript:void(0)', :class => 'btn btn-default save generic' %>
    </span>
  </h3>
<% end %>

<%= render 'form' %>
