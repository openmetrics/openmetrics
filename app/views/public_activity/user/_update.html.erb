<% if activity.trackable %>
    <% if activity.parameters and activity.parameters[:current_user_id] == activity.trackable.id %>
        updated his profile
    <% else %>
        updated profile of user <%= link_to activity.trackable.username, activity.trackable %>
    <% end %>
<% else %>
    which has since been removed
<% end %>

