<h3>
    <%= raw t('om.views.system.index.title') %>
    <%= link_to raw('<span class="glyphicon glyphicon-plus"></span> ' + t('om.views.system.index.new_system')),
            new_system_path, :class => 'btn btn-primary pull-right' %>
    <small>no filters active</small>
</h3>


<%= raw htmltable_for @systems, { :html_class => 'spacer', :join => 'running_services', :actions => { :edit => true, :show => true } } %>