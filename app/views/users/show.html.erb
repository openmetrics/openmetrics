<div class="row">

    <div class="col-md-8">
      <h3>
          <%= raw(gravatar_image_tag(current_user.email, :class => "avatar gravatar-l right", :gravatar => {:size => 120}))%>
          <small>
          <% if @user == current_user %>
              <%= "Showing User #{@user.username}" %><br>foo
          <% else %>
              <%= "Showing User #{@user.id}" %>
          <% end %>
          </small>
      </h3>
      <button class="btn btn-success"><span class="fa fa-bug"></span> Edit Profile</button>
      <hr>
      <h3>User activity</h3>
    </div>
    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading">Your items</div>
        <div class="panel-body">
            <h1>fooo</h1>
        hallo
        </div>
        </div>

    </div>
</div>

