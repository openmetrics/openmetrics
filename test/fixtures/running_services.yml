# base system
rs_om_server:
  service: om_server
  system: localhost
  description: The Openmetrics server itself.

rs_om_client:
  service: om_agent
  system: localhost
  description: Gathers statistics and data of interest.