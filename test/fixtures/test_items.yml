# bash
bashsleep30:
  name: selftest bashsleep30
  description: Sleep for 30 seconds and exit with a return code indicating success.
  type: TestScript
  format: bash
  markup: |
    sleep 30

bashexitsuccess:
  name: selftest bashexitsuccess
  description: Exit with a return code indicating success.
  type: TestScript
  format: bash
  markup: |
    exit 0

bashexiterror:
  name: selftest bash exit error
  description: Exit with a return code indicating an error (integer greater zero).
  type: TestScript
  format: bash
  markup: |
    exit 42

